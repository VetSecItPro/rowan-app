# Admin Configuration
# Admin Session Encryption (CRITICAL SECURITY)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be exactly 64 hex characters (32 bytes)
ADMIN_SESSION_SECRET=your_64_character_hex_string_here

# Supabase Configuration (existing)
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx...
SUPABASE_SERVICE_ROLE_KEY=eyJxxx...

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Resend Email
RESEND_API_KEY=re_xxx...

# Google Gemini AI for Daily Digest
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Cron Job Security
# Generate a random secret for securing cron endpoints
# Example: openssl rand -base64 32
CRON_SECRET=your_secure_random_string_here

# Internal API Security
# Generate a random secret for securing internal API calls (e.g., data export generation)
# Example: openssl rand -base64 32
INTERNAL_API_SECRET=your_internal_api_secret_here

# Upstash Redis Rate Limiting
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxx...

# Sentry Error Tracking
# Get these values from https://sentry.io
# 1. DSN: Settings → Projects → [Your Project] → Client Keys (DSN)
#    Format: https://abc123@o123456.ingest.sentry.io/456789
NEXT_PUBLIC_SENTRY_DSN=

# 2. Organization Slug: Look at your URL https://sentry.io/organizations/YOUR-ORG-SLUG/
#    Or: Settings → General Settings
SENTRY_ORG=

# 3. Project Name: Settings → Projects (usually "rowan-app")
SENTRY_PROJECT=

# 4. Auth Token: User Settings (profile icon) → Auth Tokens → Create New Token
#    Scopes needed: project:read, project:releases, org:read
SENTRY_AUTH_TOKEN=

# 5. Environment (optional - defaults to NODE_ENV)
NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# =============================================================================
# Push Notifications (Firebase Cloud Messaging v1 API)
# =============================================================================
# Required for sending push notifications to iOS and Android devices.
# Uses the FCM HTTP v1 API (the legacy API was deprecated June 2024).

# Firebase Service Account - JSON string of the service account key file
# Get from: Firebase Console → Project Settings → Service accounts → Generate new private key
# Paste the entire JSON content as a single-line string:
FIREBASE_SERVICE_ACCOUNT=

# Alternative: Expo Push Notifications (if using Expo)
# Get from: https://expo.dev/accounts/[account]/settings/access-tokens
# EXPO_ACCESS_TOKEN=

# =============================================================================
# Test Endpoints (Development Only)
# =============================================================================
# WARNING: NEVER set this in production! Test endpoints can send arbitrary emails
# and expose cookie metadata. Even when set, admin auth is required in production.
# ALLOW_TEST_ENDPOINTS=true

# =============================================================================
# Node Environment
# =============================================================================
NODE_ENV=development
